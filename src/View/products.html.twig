<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twig</title>
</head>
<body>

    {% block content %}
        <ul>{% for product in products %}
                {{ product }},
                {% if loop.index is divisible by(3) %} <br>
                {% endif %}
            {% endfor %}
        </ul>
        {{ dump() }}
    {% endblock %}

    {% block javascript %}{% endblock %}
</body>
</html>